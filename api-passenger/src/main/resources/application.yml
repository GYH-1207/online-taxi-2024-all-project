server:
  port: 8081
spring:
  application:
    name: api-Passenger
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        service: api-Passenger
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    password: redis